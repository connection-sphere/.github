# Seed Resoures

```bash
export RUBYLIB=~/code1/sdk && \
cd ~/code1/secret/mass && \
ruby push-profiles.rb --server_url https://connectionsphere.com --api_key <su api key>
```

Edit the `manifest.rb` script when you want to register new configurations.